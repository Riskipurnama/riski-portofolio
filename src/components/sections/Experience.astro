<section id="experience" class="experience">
  <div class="section-label">
    <span>05</span>
    PROFESSIONAL EXPERIENCE
  </div>

  <div class="experience-header">
    <h2>
      Experience that
      <span>speaks for itself.</span>
    </h2>

    <p>
      Practical experience in data processing and administration,
      developed through remote work and real operational needs.
    </p>
  </div>

  <div class="experience-card">

    <div class="experience-top">
      <div>
        <span class="experience-number">01</span>

        <h3>Data Processing</h3>

        <p class="company">
          SPDN 28.311.08 · PT Asal Masih Untung Kerjakan
        </p>
      </div>

      <div class="experience-date">
        <span>REMOTE</span>
        <strong>2024 — PRESENT</strong>
      </div>
    </div>

    <div class="experience-content">

      <div class="experience-description">
        <p>
          Responsible for processing and managing operational data,
          including data input, updating, checking, validation,
          document management, and other administrative tasks.
        </p>

        <p>
          Work is performed remotely while following operational
          requirements, instructions, and assigned responsibilities.
        </p>
      </div>

      <div class="experience-skills">
        <span>Data Processing</span>
        <span>Data Administration</span>
        <span>Data Validation</span>
        <span>Microsoft Excel</span>
        <span>Spreadsheet</span>
      </div>

    </div>

    <div class="experience-footer">
      <span>
        PROFESSIONAL DOCUMENT
      </span>

      <a
        href="/documents/surat-pengalaman-kerja.pdf"
        target="_blank"
        rel="noopener noreferrer"
      >
        View Experience Letter
        <span>↗</span>
      </a>
    </div>

  </div>
</section>

<style>
  .experience {
    position: relative;

    padding: 8rem 7%;

    border-top: 1px solid rgba(255, 255, 255, 0.08);

    overflow: hidden;
  }

  .experience::before {
    content: '';

    position: absolute;

    width: 600px;
    height: 600px;

    left: -300px;
    top: 25%;

    background: radial-gradient(
      circle,
      rgba(255, 255, 255, 0.045),
      transparent 65%
    );

    filter: blur(50px);

    pointer-events: none;
  }

  .section-label {
    position: relative;

    display: flex;
    align-items: center;
    gap: 12px;

    margin-bottom: 4rem;

    font-size: 0.65rem;
    letter-spacing: 0.25em;

    color: rgba(255, 255, 255, 0.4);
  }

  .section-label span {
    color: rgba(255, 255, 255, 0.85);
  }

  .experience-header {
    position: relative;

    display: grid;
    grid-template-columns: 1.2fr 0.8fr;

    gap: 5rem;

    align-items: end;

    margin-bottom: 5rem;
  }

  .experience-header h2 {
    margin: 0;

    font-size: clamp(3.5rem, 7vw, 7rem);

    line-height: 0.88;

    letter-spacing: -0.065em;

    font-weight: 600;
  }

  .experience-header h2 span {
    display: block;

    color: transparent;

    -webkit-text-stroke: 1px rgba(255, 255, 255, 0.6);
  }

  .experience-header p {
    max-width: 420px;

    margin: 0;

    font-size: 0.95rem;

    line-height: 1.8;

    color: rgba(255, 255, 255, 0.5);
  }

  .experience-card {
    position: relative;

    padding: 2rem;

    border: 1px solid rgba(255, 255, 255, 0.12);

    background: rgba(255, 255, 255, 0.025);

    border-radius: 12px;

    box-shadow:
      0 30px 80px rgba(0, 0, 0, 0.35),
      inset 0 1px 0 rgba(255, 255, 255, 0.04);

    transition:
      transform 0.5s ease,
      border-color 0.5s ease,
      background 0.5s ease;
  }

  .experience-card:hover {
    transform: translateY(-5px);

    border-color: rgba(255, 255, 255, 0.22);

    background: rgba(255, 255, 255, 0.035);
  }

  .experience-top {
    display: flex;

    justify-content: space-between;

    gap: 2rem;

    padding-bottom: 2rem;

    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
  }

  .experience-number {
    display: block;

    margin-bottom: 1rem;

    font-size: 0.65rem;

    letter-spacing: 0.15em;

    color: rgba(255, 255, 255, 0.35);
  }

  .experience-top h3 {
    margin: 0;

    font-size: clamp(2.2rem, 4vw, 4rem);

    line-height: 1;

    font-weight: 500;

    letter-spacing: -0.04em;
  }

  .company {
    margin: 1rem 0 0;

    font-size: 0.8rem;

    color: rgba(255, 255, 255, 0.45);
  }

  .experience-date {
    display: flex;

    flex-direction: column;

    align-items: flex-end;

    gap: 8px;

    white-space: nowrap;
  }

  .experience-date span {
    font-size: 0.6rem;

    letter-spacing: 0.2em;

    color: rgba(255, 255, 255, 0.3);
  }

  .experience-date strong {
    font-size: 0.7rem;

    font-weight: 400;

    letter-spacing: 0.08em;

    color: rgba(255, 255, 255, 0.65);
  }

  .experience-content {
    display: grid;

    grid-template-columns: 1fr 0.7fr;

    gap: 4rem;

    padding: 2rem 0;
  }

  .experience-description p {
    max-width: 600px;

    margin: 0 0 1rem;

    font-size: 0.9rem;

    line-height: 1.8;

    color: rgba(255, 255, 255, 0.5);
  }

  .experience-description p:last-child {
    margin-bottom: 0;
  }

  .experience-skills {
    display: flex;

    flex-wrap: wrap;

    align-content: flex-start;

    gap: 8px;
  }

  .experience-skills span {
    padding: 0.45rem 0.7rem;

    border: 1px solid rgba(255, 255, 255, 0.12);

    border-radius: 999px;

    font-size: 0.65rem;

    color: rgba(255, 255, 255, 0.6);

    transition:
      background 0.3s ease,
      border-color 0.3s ease,
      color 0.3s ease,
      transform 0.3s ease;
  }

  .experience-skills span:hover {
    background: rgba(255, 255, 255, 0.08);

    border-color: rgba(255, 255, 255, 0.3);

    color: white;

    transform: translateY(-2px);
  }

  .experience-footer {
    display: flex;

    align-items: center;

    justify-content: space-between;

    gap: 1rem;

    padding-top: 1.5rem;

    border-top: 1px solid rgba(255, 255, 255, 0.08);
  }

  .experience-footer > span {
    font-size: 0.6rem;

    letter-spacing: 0.18em;

    color: rgba(255, 255, 255, 0.3);
  }

  .experience-footer a {
    display: inline-flex;

    align-items: center;

    gap: 10px;

    padding: 0.7rem 1rem;

    border: 1px solid rgba(255, 255, 255, 0.18);

    border-radius: 999px;

    color: white;

    text-decoration: none;

    font-size: 0.7rem;

    transition:
      background 0.3s ease,
      border-color 0.3s ease,
      transform 0.3s ease;
  }

  .experience-footer a:hover {
    background: rgba(255, 255, 255, 0.08);

    border-color: rgba(255, 255, 255, 0.35);

    transform: translateY(-2px);
  }

  .experience-footer a span {
    transition: transform 0.3s ease;
  }

  .experience-footer a:hover span {
    transform: translate(3px, -3px);
  }

  @media (max-width: 768px) {
    .experience {
      padding: 7rem 6%;
    }

    .section-label {
      margin-bottom: 3rem;
    }

    .experience-header {
      grid-template-columns: 1fr;

      gap: 2rem;

      margin-bottom: 4rem;
    }

    .experience-header h2 {
      font-size: clamp(3rem, 13vw, 5rem);
    }

    .experience-card {
      padding: 1.3rem;
    }

    .experience-top {
      flex-direction: column;

      gap: 1.5rem;
    }

    .experience-date {
      align-items: flex-start;
    }

    .experience-content {
      grid-template-columns: 1fr;

      gap: 2rem;
    }

    .experience-footer {
      flex-direction: column;

      align-items: flex-start;
    }
  }
</style>